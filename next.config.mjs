/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['imgpanda.com', 'i.ibb.co'],
  },
  compiler: {
    styledComponents: true,
  },
};

export default nextConfig;
